lib_sources = files('find_primes.cc')

find_primes_lib = library(
    'find_primes',
    include_directories : inc,
    sources : lib_sources,
)

# find_primes_dep = declare_dependency(
#     include_directories : inc,
#     link_with : find_primes_lib,
# )
